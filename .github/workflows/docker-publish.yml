name: Docker Publish

on:
  push:
    tags:
      - "v*"

jobs:
  push:
    name: Build and push ghcr.io/soundmatt/java-fusa
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/soundmatt/java-fusa
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ github.ref_name }}
            GIT_COMMIT=${{ github.sha }}
            BUILD_DATE=${{ github.event.head_commit.timestamp }}
            SPEC_VERSION=1.15.0
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Notify FuSaOps
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.FUSAOPS_DISPATCH_TOKEN }}
          repository: SoundMatt/FuSaOps
          event-type: xfusa-released
          client-payload: '{"tool":"java-FuSa","version":"${{ github.ref_name }}"}'
        continue-on-error: true
